//#include"student_dclink.h"
#include<stdio.h>
int main()
{
	printf("请666");
//	char num[10]={0};
//	int select = 1;
//	Student_dclink *head = {0};
//	head = create_student_dclink();
//	head = (Student_dclink *)malloc(sizeof(Student_dclink));
//	head ->next = NULL;
//	head ->prev = NULL;
//	printf("请输入你想要的操作：1.显示学生表。2.插入学生。3.删除学生。4.寻找学生:");
	
/*	
	while(select>0&&select<=4)
	{
		printf("请输入你想要的操作：1.显示学生表。2.插入学生。3.删除学生。4.寻找学生:");
		scanf("%d",&select);
	switch(select)
	{
		case 1:
		show_student_dclink(head);
				break;
		
		case 2:
		insert_student_dclink(head);
				
				break;
				
		case 3:
		printf("请输入删除学生的学号:");
				scanf("%s", num);
				delete_student_dclink(head, num);
				break;
				
		case 4:
		printf("请输入要查找的学生的学号:");
				scanf("%s", num);
				search_student_dclink(head,num);
				break;
				default:break;
	}

	}
	destroy_student_dclink(head);
	free(head);
	*/
	return 0;
}
